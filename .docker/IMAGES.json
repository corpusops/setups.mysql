{
    "images": [
        {
            "tag": "corpusops/mysql:5.7",
            "builder_type": "docker",
            "dockerfile": "Dockerfile",
            "extra_args": "--build-arg COPS_IMG={tag}"
        }
    ]
}
